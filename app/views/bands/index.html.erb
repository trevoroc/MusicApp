<h2>Bands</h2>

  <br>

  <ul>
    <% @bands.each do |band| %>
      <li><%= link_to "#{band.name}", band_url(band) %></li>
      <!-- Later: replace with link to band's show page -->
    <% end %>
  </ul>

<%= button_to "New Band", new_band_url, method: :get %>
